with pkg_tareas; use pkg_tareas;
procedure ejercicioTareas is
   tarea1 : T_Tarea(0,9);
   tarea2 : T_Tarea(10,99);
   tarea3 : T_Tarea(100,999);
begin
   null;
end ejercicioTareas;

-- Ejercicio 7

-- a) El programa principal instancia 3 T_Tarea con los parámetros a, b de tipo Integer.
--    Cada tarea genera 20 nº aleatorios entre los parámetros especificados.

-- b) Si comentamos la sentencia delay, los numeros se generan de forma más consecutiva.
--    En cambio, si no la comentamos los numeros se intercalan con más frecuencia.

-- c) Estas sentencias se ejecutan al inicio y pueden utilizarse para mostrar mensajes iniciales, leer ficheros, etc.
